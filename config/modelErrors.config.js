const REQUIRED_FIELD = 'this field is required'
const MIN_LENGTH = 'invalid length'
const INVALID_EMAIL = 'please enter a valid email'

module.exports = {
  REQUIRED_FIELD, MIN_LENGTH, INVALID_EMAIL
}